# Hosting requirements: identical to requirements.txt except the embedding
# backend — fastembed (ONNX) replaces sentence-transformers/torch so the app
# fits Render's 512MB free tier (run with EMBED_BACKEND=fastembed).
# Verified 20/20 top-5 ranking agreement with the torch backend on QA queries.
claude-agent-sdk>=0.2.111
anthropic>=0.79.0
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-dotenv>=1.0.0
numpy>=1.26.0
fastembed>=0.4.0
pymupdf>=1.28.0
